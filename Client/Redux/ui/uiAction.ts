import { UiType } from "./uiType";

// =================================================================

export const sidebarToggle = () => {
  return { type: UiType.SIDE_BAR_TOGGLE };
};

// =================================================================

export const sidebarCart = () => {
  return { type: UiType.SIDE_CART_TOGGLE };
};

// =================================================================
