mutation CreateProduct($name: String!, $price: Float!, $description: String!, $stock: Float!) {
  createProduct(input: { name: $name, price: $price, description: $description, stock: $stock }) {
    _id
    name
    price
    description
    stock
    promotion
  }
}
