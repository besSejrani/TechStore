import React from "react";

import CartSection from "../Components/Cart";

const Cart = ({ history }) => {
  return (
    <>
      <CartSection history={history} />
    </>
  );
};

export default Cart;

// =================================================================
